ambigious when is space : ila ay haja fih tathwl lspace nkhli lklma kima hiya
some expanding cases $ and num right after stop at first number 
#!/bin/bash

echo "\$? = $?"   # Exit status of the last command
echo "\$! = $!"   # Process ID of the last background command
echo "\$@ = $@"   # All arguments to the script (separated)
echo "\$* = $*"   # All arguments to the script (one single string)

dup2 in pipe protect